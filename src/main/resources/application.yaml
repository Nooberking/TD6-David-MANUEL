spring:
  datasource:
    url: jdbc:postgresql://tcp-mo4.mogenius.io:54718/covid-db
    username: test-user
    password: password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
  liquibase:
    change-log: db/changelog/db.changelog-master.yaml

